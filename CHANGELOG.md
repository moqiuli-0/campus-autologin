# 更新日志（Changelog）

所有显著变更记录在此文件中。格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)。

## [1.18] - 2026-08-31

### UI 微调
- 卡片与弹窗底色统一改为浅灰（#F2F2F2，纯白背景上更柔和）
- 首页三个操作按钮间距收紧（12→6dp）；账号卡展开时按钮跟随内容排布、不再固定页面底端（折叠时仍固定底端）

## [1.17] - 2026-08-31

### UI 优化
- 设置页重构：功能按类型拆分为「检测 / 运行环境 / 主题外观 / 离校模式 / 日志与关于」五个子页面，设置主页改为入口列表
- 首页「立即检测并登录 / 设置 / 离校模式」三个按钮固定在页面底端（样式不变）
- 首次填写并保存账号密码后自动打开后台监控
- 背景固定为纯白（不随主题变化、不可自定义）；主题颜色新增纯黑，默认主题改为纯黑
- 文案精简：离校模式相关文案、"记住位置"、电脑版登录说明、ADB 说明、运行环境检查项；删除"运行环境不满足时置顶"的特殊排序

### 修复（顺带修复备忘录随版处理）
- WebView 混合内容策略由全局放行收窄为兼容模式（H1）
- 删除未使用的死代码（M4）；两处非空断言改为就地安全处理（M8）
- README 重复章节删除（M6）；CHANGELOG"深色适配"措辞修正为"浅色主题"（M7）

## [1.16] - 2026-08-31

### 新增
- 手动指认校园网：关键词规则没覆盖的 WiFi，可在主页状态卡一键点「这是校园网，记住它」，结果写入 WiFi 校园网判断记忆库（与自动学习共用，最多 40 条），记住后自动立即重新检测并登录；手动指认与关键词规则等效，未配置任何关键词时也可以仅靠手动指认工作

### 修复
- 「联系作者」卡片的项目仓库地址更新为当前公开仓库（旧地址已失效）
- 隐私说明中关于联系方式的描述与实际功能不一致的文案修正

### 优化
- WiFi 未命中关键词规则时的状态文案更准确（"未识别为校园网"，不再武断显示为非校园网）
- 清理重复导入

## [1.15] - 2026-08-30

首个公开发布版本。

### 新增
- 后台自动认证：连接需要网页认证的校园网 WiFi 时，自动检测认证页并代填账号密码完成登录
- SSID 关键词规则：在设置中添加你的校园网 WiFi 名称关键词（支持部分匹配、忽略大小写），可随时增删
- 认证页自动学习：首次遇到陌生认证页时自动识别并记住该 WiFi，无需重复判断
- 非校园网保护：认证页与所配规则不符时绝不发送账号密码，并可一键用浏览器打开认证页手动登录
- 位置围栏（可选）：可采集"学校位置"，登录前核对是否在校（默认关闭，仅本地比对不上传）
- 离校模式：一键停止后台监控与开机自启并关闭应用，下次打开自动恢复
- 电脑版登录模拟：可切换桌面浏览器标识，配合"手机版/电脑版各一台设备"的校园网限制
- 检测间隔自定义（15–3600 秒）、首次连接立即检测
- 通知栏实时状态（尝试用时/已连接/失败原因）、登录历史、双档位日志与导出
- 主题颜色自定义（预设 + RGB/HEX）、浅色主题、隐私声明、ADB 授权引导

### 说明
- 账号密码使用安卓系统级加密仅存本机，不上传任何服务器
- 联系方式：dachaiquan@foxmail.com
